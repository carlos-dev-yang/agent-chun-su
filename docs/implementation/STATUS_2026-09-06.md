# Implementation Handoff — 2026-09-06

The implementation scope requested on September 6 was Phase 0 through Phase 5, stopping before Jira. Those command paths are now prepared and committed locally. **The real executor, live-mail usefulness and unattended-operation milestones are not complete.**

## Prepared scope

| Area | Available now |
|---|---|
| Local core | Go CLI, embedded SQLite, private configuration, durable jobs/attempts/events, one controller owner and immutable artifacts |
| Mail execution | Saved envelopes, versioned workgroup guide/schema, pinned packages, scoped MCP source reads, Codex adapter, contract validation and Markdown reports |
| Evaluation | Versioned cases/rubrics, independent judgments and findings, comparisons including failures/unknowns, inactive candidates and human adoption/rejection/rollback |
| Gmail | Desktop OAuth/PKCE/loopback implementation, Keychain references, bounded read APIs, MIME normalization, fixed date windows, resumable collection, prior local report selection and duplicate admission protection |
| Local operations | Queue pause/cancel/resolve/retry, publication/coverage repair, disabled interval schedules, optional LaunchAgent lifecycle commands, consistent backup/restore and reviewed run-content retention |

The AI executor receives the permitted work package and source tool responses. Operational controls, evaluation expectations, account bindings and credentials remain outside it. Experimental reports do not advance production coverage or replace production history. Human feedback is stored independently and is not silently adopted as a new rule or operational fact.

No Jira connector, mailbox mutation, external report messaging, public release pipeline or development-work executor was added. Phase 6 and later remain unstarted.

## Validation that actually ran

- Native Go build with CGO disabled and whole-module vet for this cross-cutting implementation.
- A Linux/amd64 cross-build with CGO disabled. This is compilation evidence only; Linux execution, Gmail secrets, service behavior and isolation have not been validated.
- Synthetic CLI/persistence walkthroughs for input integrity, queue ownership, waits, explicit cancellation/resume, budget exhaustion and additive schema upgrade.
- Real MCP protocol calls with synthetic stored attempts: scoped inventory, allowed/denied lookup, revocation journaling, budget retention after server restart and manifest/input binding.
- Command-level Codex permission-profile probes for marker file reads/writes, local TCP and the management socket. These are narrower than the actual model invocation boundary.
- Synthetic feedback/proposal/comparison checks, including refusal of validation-actor adoption and separation of Gmail candidate admission/coverage.
- Saved MIME/query/time-window checks, synthetic publication checkpoints, prior-report time/source selection and terminal-safe rendering.
- Consistent backup including committed WAL data, hash/tombstone verification, separate-root restore, stale retention refusal, repeated deletion application and disabled restored controls.
- Rendered LaunchAgent plist parsing, without installation or activation.

No generated test suite was added. Existing project test files were absent. Seeded executor-result records used in publication checks are explicitly synthetic and are **not** evidence that an AI produced or understood the report. One short paused-core sample used 23,168 KiB RSS; it is not a benchmark or a long-duration reliability claim.

Detailed evidence is in [Phase 1](../validation/PHASE_01_FOUNDATION.md), [Phase 2](../validation/PHASE_02_SAVED_MAIL.md), [Phase 3](../validation/PHASE_03_FEEDBACK.md), [Phase 4](../validation/PHASE_04_GMAIL.md), and [Phase 5](../validation/PHASE_05_OPERATIONS.md).

## Remaining inputs and evidence

1. **Executor choice/account.** The installed Codex CLI reports an existing ChatGPT login. The user's response about using it is pending; no real model call has been made. Pin a model when comparing candidates.
2. **Actual executor boundary.** The local feature inspection reports `unified_exec` enabled despite its disable override, while `shell_tool` is disabled. This alone proves neither exposure nor isolation. Confirm actual available tools and the private-file/credential/control boundary using synthetic sources. `executor.live_mail_approved` stays false until that walkthrough and an explicit human decision; executor changes and restores reset it.
3. **Keychain blocker cleared on the pilot host.** Following the user's report of a verified marker check, the connection preflight and real credential write/read verification succeeded. A new CLI process retrieved the stored credentials and completed a refresh/account check. The earlier `-25293` failure is preserved as diagnosis history; its precise macOS cause was not established. There is no plaintext fallback. See the [connection evidence](../validation/PHASE_04_GMAIL.md#successful-pilot-connection-and-refresh).
4. **Gmail account connected.** The selected account completed OAuth with exactly `gmail.readonly`; an independent `gmail check` matched the account and reported no message bodies read. The bound policy is recent seven-day inbox mail, 20 messages per batch, related-thread history disabled, `Asia/Seoul`, manual retention and local reports. No message query or collection has run. Credential files and private connection records remain outside Git, and the user's ignore-file edits are preserved. Provider message reads and live-report readiness still need validation.
5. **Personal usefulness and operating policy.** Review changes-only versus changes-plus-open-items semantics, draft expectations and one real baseline/candidate result. The current envelope is not an account-wide open-task tracker. Decide interval/timezone and accept the operating policy before enabling schedules or a service.

Provider message reads/pagination, reauthorization/revocation and long-duration refresh, actual AI output and cancellation, user report quality, launchd lifecycle, real sleep/wake and long-duration operation remain unverified. Initial OAuth, credential storage and one fresh-process refresh/account check passed; these do not complete the outstanding milestones.

## Start and resume

Build/setup instructions are in the [README](../../README.md). No DB server, queue broker or web dashboard is required. The core binary embeds SQLite; the selected AI executor and Gmail authorization have their own prerequisites.

Read [Gmail pilot setup](../setup/GMAIL_PILOT.md) for the account flow and query/history previews, and the [local operations runbook](../setup/LOCAL_OPERATIONS.md) for blocked work, service configuration and data recovery. The next useful execution is the synthetic actual-executor walkthrough, followed by a small scoped Gmail reporting pilot using the now-connected account after the disclosure decision. Background activation remains separate from code preparation.

## Local Git checkpoints

| Commit | Work unit |
|---|---|
| `0067f1d` | Phase 0 core contracts and Gmail scope |
| `b39edf1` | Phase 1 durable local CLI foundation |
| `87706ff` | Phase 2 saved-mail pipeline, scoped tools and lifecycle |
| `54c4aed` | Phase 3 feedback and controlled candidates |
| `97f6ae6` | Phase 4 Gmail connector preparation |
| `3ab66c8` | Phase 5 report publication and consistent backup |
| `a8f3f7d` | Phase 5 scheduling, management, service preparation and retention |

Final integration corrections and this handoff accompany the subsequent commit with subject `fix(P2-03 P3-05 P4-07 P5-03): tighten mail execution and recovery`. No remote was configured, pushed or published. Runtime databases, mail data, credentials and build outputs are excluded from commits.
