# chun-su 설계 문서 (통합본)

실행기를 블랙박스로 둔 AI 에이전트 워크플로우. 기준은 사람이 파일로 관리하고, 시작과 끝은 커널이 강제하고, 그 사이는 계약으로만 건넌다.

- 상태: 설계 검토 단계. 구현은 초기 러너까지 (11절)
- 작성: 2026-09-02, 대화형 설계 세션에서 나온 결정을 시간순으로 정리
- 읽는 법: 처음이면 1 → 2 → 3 → 4 → 10 순으로. 3절이 의사결정 기록이고, 나머지는 그 결정이 만든 구조다
- 이 파일이 원본이다. `docs/00~08` 은 단계별 상세이고 이 문서와 충돌하면 이 문서를 따른다

---

## 목차

1. 배경과 문제
2. 원칙
3. 의사결정 기록
4. 구조 개요
5. 시작 구조: 카드, 컴파일, 팩, 승인
6. 실행기 경계: 컨테이너, 어댑터, 셔틀
7. 끝 구조: 수집, 게이트, 판정, 기록
8. 상태 기계
9. 평가와 갱신
10. 시나리오 세 개
11. 현재 구현 상태와 거리
12. 계약 목록과 매니페스트
13. 불변식과 강제 수단
14. 열린 결정과 검토 질문
15. 다음 단계
- 부록 A. 용어
- 부록 B. email-triage 규칙의 껍질·핵 분해
- 부록 C. Hermes 설치 기록

---

## 1. 배경과 문제

### 출발점
개인 에이전트 세팅 문서(`hermes-agent-setup.md`)가 있었다. 정책과 지식의 원본은 볼트에 두고, Hermes 는 루프와 스킬만 맡기고, 게이트는 작업 에이전트 밖 양 끝에 두고, 모델의 자기채점은 게이트가 아니라는 전제였다. 이 문서는 그 전제를 실제 구조와 코드로 옮기는 과정에서 나온 결정들을 담는다.

### 관찰한 문제
- 모델이 강해질수록 훅이나 프롬프트로 작업 중간에 교정하는 방식의 효용이 떨어진다. 이미 경로를 정한 모델을 붙잡으면 토큰은 크게 늘고 품질은 거의 오르지 않는다. 같은 모델에 강제적 하네스로 교정과 피드백을 반복했을 때 토큰 5배에 품질 1.1배라는 결과를 본 논문이 있었다
- 제약이 많아질수록 모델은 우회하거나 과잉 준수한다
- 사람이 매 턴 개입하면 사람이 병목이 된다. 개입을 줄이면 통제를 잃는다

### 목표
- 기준(정책, 스키마, 스킬, 툴)은 사람이 관리한다
- 하네스는 얇게 둔다
- 작업은 AI 에게 맡기고 중간에 끼어들지 않는다
- 프롬프트로 통제하지 않고, 중요한 지점에 게이트를 둔다
- 작업 완료 후 골든셋으로 채점하고, 그 결과로 기준을 갱신하는 피드백 루프를 만든다
- 개발 영역(e2e 게이트)은 전체 구조가 완성된 뒤 따로 얹는다

---

## 2. 원칙

1. **기준은 사람이 관리한다.** 원본은 파일이다. 실행기는 복사본만 읽는다.
2. **하네스는 얇다.** 커널은 순서와 계약만 강제한다. 판단은 하지 않는다.
3. **작업은 AI 에게 맡긴다.** 중간에 끼어들지 않는다. 재시도는 정해진 횟수만.
4. **통제는 게이트로 한다.** 프롬프트로 "지켜라"라고 쓰지 않는다. 안 지키면 게이트에서 떨어지게 한다.
5. **품질은 앞이 만들고, 게이트는 뒤에서 걸러내고, 골든셋은 그 뒤에서 잰다.** 세 역할을 섞지 않는다.

| 역할 | 답하는 질문 | 어디에 | 실패하면 |
|---|---|---|---|
| 기준 (rules, schemas, skills, tools) | 어떻게 해야 잘 하는가 | 워킹그룹 폴더, git | 산출물 품질 저하 |
| 게이트 | 최소 요건을 지켰는가 | 커널, 매니페스트 선언 | 수용 거부, 재시도 |
| 골든셋 평가 | 통과한 것이 얼마나 좋은가 | 워킹그룹 golden, 주간 루틴 | 점수 하락, 기준 수정 후보 |

게이트는 잘못을 잡지만 잘한 것을 만들지 못한다. 품질 투자는 기준에 한다.

---

## 3. 의사결정 기록

시간순. 각 항목은 맥락, 검토한 선택지, 결정, 이유, 결과로 적는다. 기각한 선택지도 남긴다.

### D1. 통제 지점을 양 끝으로 옮긴다
- **맥락**: 중간 개입(훅, 프롬프트, 사람)이 비효율적이라는 관찰
- **선택지**: (a) 프롬프트에 규칙을 길게 적는다 (b) 훅으로 매 도구 호출을 검사한다 (c) 시작 전 기준 정의 + 종료 후 게이트
- **결정**: (c)
- **이유**: (a)는 모델이 강해질수록 희석·충돌·우회가 늘고, (b)는 토큰 대비 품질 개선이 미미하며 모델이 검사를 넘는 쪽으로 수렴한다. 양 끝 통제는 실행 중 개입이 없어 토큰이 늘지 않고, 사람의 개입 주기가 매 턴에서 매 주로 바뀐다
- **결과**: 게이트는 실행기 밖에 있어야 한다는 요구가 생김 (D4)

### D2. 기준의 원본은 파일이고 사람이 소유한다
- **맥락**: 에이전트 제품들은 스스로 스킬을 만들고 메모리를 쌓는 "자기진화"를 핵심 기능으로 둔다
- **선택지**: (a) 에이전트가 경험에서 기준을 스스로 축적 (b) 사람이 git 으로 기준을 관리, 에이전트는 읽기만
- **결정**: (b)
- **이유**: 기준이 두 곳(사람의 파일, 에이전트의 메모리)에 생기면 어느 것이 원본인지 알 수 없다. 실패했다고 기준이 자동으로 바뀌면 기준은 게이트를 통과하는 방향으로만 다듬어진다. AI 는 후보 요약까지, 결정은 사람
- **결과**: `MEMORY.md` 에는 경로 지도만 둔다. 스킬 자동 생성·큐레이션은 전부 끈다. 스킬에 규칙을 복사하지 않고 "rules 를 읽어라"까지만 쓴다

### D3. 골든셋은 게이트가 아니라 사후 평가다
- **맥락**: 초기 가이드에서 골든셋 피드백 루프가 Goodhart(측정 지표에 최적화되어 실제 품질이 떨어짐)로 흐를 위험을 지적함
- **반박**: 골든셋 루프는 자동화가 아니다. 사람이 골든셋 결과를 보고 직접 정책과 워크플로우를 고친다. 골든셋은 통과 기준이 아니라 "얼마나 잘 통과했는지"를 재는 평가 기준이고, 다음 작업 품질을 올리는 지침의 단초다
- **결정**: 골든셋은 수용을 막지 않는다. 기록이 닫힌 뒤 비동기로 채점한다. 채점 결과는 사람이 읽고 기준을 고치는 입력이다. 자동 기준 갱신 없음
- **남은 주의점**: 골든 케이스가 실제 유입 분포를 대표해야 한다. 머리로 만들지 말고 runs 의 실패·애매 건을 익명화해 승격한다
- **결과**: 평가 단계가 판정·기록과 분리됨 (7절, 9절)

### D4. 게이트는 스킬이 아니라 실행기 밖의 코드다
- **맥락**: 원안에는 `done-check` 스킬이 있었다
- **문제**: 스킬은 모델이 실행한다. 건너뛰거나 형식적으로 넘길 수 있다. "스스로 검증했음"은 게이트가 아니다
- **결정**: 게이트는 러너(후에 커널)가 실행기 바깥에서 호출한다. 실행기는 게이트를 부르지 않는다
- **결과**: 스킬 5개 안이 2개(kb-read, office-draft 성격의 email-triage)로 줄고, done-check·fail-card 는 커널의 단계가 됨

### D5. 기준을 워킹그룹 단위로 묶는다
- **맥락**: 스키마, 규칙, 골든 케이스를 어디에 둘 것인가
- **결정**: 작업 영역 하나마다 `workgroups/<name>/` 에 rules, schemas, skills, tools, gates, golden 을 모은다. 외부에서 주는 작업 설계(카드)는 간단한 지침과 그 작업에서 필수로 지킬 조건만
- **이유**: 품질을 만드는 것은 워킹그룹 안의 기준이다. 개발 워킹그룹이면 스키마 생성 원칙, 컴포넌트 간 규칙, 디자인 룰이 여기 들어간다. 같은 구조를 복제하면 사무와 개발이 같은 루프를 쓴다
- **결과**: `_template` 워킹그룹과 `email-triage` 예시 작성

### D6. 카드는 짧게, 엄격함은 기준 파일에
- **맥락**: "시작 전에 엄격하고 좋은 설계"를 매 작업마다 사람이 쓰면 병목이 입구로 옮겨질 뿐이라는 위험 지적
- **정리**: 매 작업의 설계서가 아니다. 스키마, 규칙, 골든은 워킹그룹 안에 상시 있고, 카드는 지시 한 줄과 must 두어 줄이다
- **결정**: 카드 스키마에 must 1~5개 제한. 카드 작성이 10분을 넘기거나 같은 must 가 세 카드에 반복되면 rules/schemas 로 승격
- **결과**: 실제 이메일 분류 예시로 카드·규칙·스키마·골든·게이트를 한 세트 작성해 검증

### D7. Hermes 를 설치했으나 보류한다
- **맥락**: 원안의 실행기는 Hermes Agent 였다. v0.21.0 설치 후 자기진화·Skills Hub·크론·게이트웨이를 모두 끄고 CLI 단발 호출만 쓰도록 설정
- **관찰**: Hermes 의 정체성은 "self-improving agent" 와 메신저 게이트웨이다. 우리 설정은 그 반대편이다. 다 끄면 남는 것은 도구 루프 + 스킬 로더 + 세션 기록이고, 이것은 `claude -p` 나 `codex exec` 도 한다. 골든셋 채점 기능은 Hermes 에 없다
- **선택지**: (a) Hermes 유지 (b) `claude -p` 로 교체 (c) AWS AgentCore 로 이동
- **결정**: Hermes 는 지우지 않고 보류. 실행기는 블랙박스로 두고 비교는 하지 않는다 (D9). AgentCore 는 시점이 이르다
- **AgentCore 판단 근거**: Policy(Cedar)·Gateway·Identity·Evaluations 는 방향이 맞지만, 정책이 파일이 아니라 AWS 리소스이고, 이터레이션 속도와 비용이 개인 테스트 단계에 맞지 않다. 사용자가 나 외에 생기거나 게이트를 인프라 층에서 우회 불가능하게 강제해야 할 때 간다. 그때도 rules/schemas/golden 은 그대로 가져가 Policy 와 Evaluations 에 매핑한다
- **결과**: 실행기에 의존하지 않는 구조가 요구사항으로 확정 (D8)

### D8. 러너가 아니라 커널이 필요하다
- **맥락**: 실행기 무관 러너를 만들었더니, 러너는 실행기 앞뒤의 검문소 두 개일 뿐이고 "시작을 책임지는 층"과 "끝을 책임지는 층"이 코드에 없다는 자각. 특히 시작 쪽은 카드에 "읽어라"고 적는 것뿐이라 프롬프트 통제였다
- **결정**: 단계가 명시된 파이프라인(컴파일 → 승인 → 실행 → 수집 → 게이트 → 판정 → 기록 → 평가 → 관찰) 과 워킹그룹 매니페스트를 갖는 작은 커널을 만든다. 러너 위에 얹지 않고 대체한다
- **만들지 않는 것**: 워크플로 엔진, 병렬 실행, 큐, 범용 게이트 DSL, 실행기 어댑터 세 개 동시
- **정당화 조건**: 실행기 둘 이상 또는 워킹그룹 둘 이상. 개발 워킹그룹이 예정돼 있어 충족

### D9. 실행기는 블랙박스, 접점은 계약으로만
- **결정**: 실행기는 `run(pack, out, budget) → exit` 하나로만 보인다. 커널이 붙잡는 것은 계약 11개(12절)다. 실행기 비교, 실행기 내부, 프롬프트 설계는 이 설계의 범위 밖
- **이유**: 실행기가 바뀌어도 기준·게이트·골든이 그대로 남아야 한다. "하네스는 그릇이다"

### D10. 사전 통제는 게이트웨이가 아니라 세계 구성이다
- **맥락**: "실행기가 사전 게이트웨이를 따르게 강제할 방법이 없다. 참조로 두고 잘 쓰길 바라야 하나"라는 의문
- **분석**: 사전 통제는 두 종류가 섞여 있었다
  1. **세계를 만드는 것**: 무엇을 읽고 쓰고 실행할 수 있는가. "하지 마라"가 아니라 "할 수 없다"로 만든다. 팩에 없는 파일은 읽을 수 없고, PATH 에 없는 도구는 부를 수 없다. 실행기의 의지와 무관
  2. **판단을 지시하는 것**: 도메인으로 판단하라, 확신 없으면 unknown. 강제 불가. 참조다
- **결정**: 1은 팩·셔틀·컨테이너로 물리적으로 강제한다. 2는 강제하지 않되, (a) 규칙마다 기계로 확인되는 껍질과 판단의 핵으로 분해하고 껍질은 스키마·invariant 로 올린다 (b) 핵은 골든 케이스로 측정한다 (c) 반복 실패하면 규칙을 사다리 위로 올린다 (D12)
- **규칙 사다리**: 불가능 > 형태 강제 > 논리 검사 > 사후 측정 > 참조. 주간 갱신의 일은 규칙을 위로 올리는 것
- **결과**: 규칙 파일에 `enforce:` 태그(capability / schema / invariant / eval / reference)를 달아 사다리 위치를 드러낸다 (부록 B)

### D11. 실행기는 컨테이너에서 돌린다
- **맥락**: 불변식 "실행기는 팩 밖을 보지 못한다"가 어댑터의 권한 번역에 기대고 있었고, 사후 검증은 파일 변경은 잡지만 읽기는 잡지 못했다
- **제안**: 격리 환경에 필요한 자원만 주고 컨테이너로 돌리면 줄 것과 주지 않을 것이 자연히 분류되고 밖의 게이트가 단순해진다
- **결정**: 채택. 컨테이너 안에는 팩(읽기 전용), out/(쓰기), 셔틀, 모델 API 로 나가는 네트워크만
- **추가 이점**: 자격증명이 컨테이너 밖에 남는다. 셔틀이 호스트 도구를 호출하는 문이 되고 토큰은 호스트가 갖는다. 실행기는 "fetch-mail 을 불렀다"만 남기고 무엇으로 인증했는지 모른다
- **해소되는 열린 결정**: 팩 격리 수준(→ 컨테이너), 셔틀 범위(→ 컨테이너 안 PATH 전체가 셔틀). 개발 워킹그룹은 레포 사본을 마운트하면 같은 틀
- **남는 구멍**: 모델 API 네트워크. 허용 목록 프록시로 엔드포인트만 연다

### D12. 판단의 핵은 남기고, 결과를 싸게 만든다
- **맥락**: "규칙·스킬·스키마·입력이 전부여도 제어 못하는 부분이 남는다"는 고민
- **분석**: 남는 것은 결함이 아니라 실행기의 존재 이유다. 기계 규칙으로 완전히 적을 수 있는 판단이면 모델이 필요 없다. 질문은 "판단을 어떻게 통제하나"가 아니라 "판단이 틀렸을 때 결과를 어떻게 싸게, 보이게, 고쳐지게 만드나"
- **결정**: 세 겹
  1. **산출물 설계로 라우팅**: `confidence`, `needs_human`, `unknown`. 모르는데 모른다고 하면 사람의 1분이고 시스템은 잘못하지 않았다. 틀렸는데 확신하면 골든 "보정" 항목이 0점을 준다. 통제 불가능한 판단을 통제 가능한 "확신의 정직함"으로 바꾼다
  2. **핵마다 골든 케이스 하나**: 게이트는 형태를, 골든은 판단을 본다. 게이트에 판단을 넣지 않는 이유는 정답이 흔들리는 검사를 통과 기준으로 두면 실행기가 검사를 넘는 쪽으로 학습하기 때문
  3. **반복되면 사다리 위로**: 껍질을 넓힌다(도메인 목록 + check-domain 셔틀 + invariant) → 라우팅을 조인다(그 유형은 needs_human 강제) → 예시를 준다. "더 신중하게"라는 문장은 추가하지 않는다
- **집중의 의미**: 팩은 참조 규칙을 강제하지 못하지만 경쟁 맥락(긴 시스템 프롬프트, 과거 메모리, 다른 파일)을 제거한다. 미준수를 "규칙을 못 봤다"가 아니라 "봤는데 판단이 달랐다"로 좁혀야 골든 결과를 규칙 수정으로 연결할 수 있다

---

## 4. 구조 개요

### 네 개 층

| 층 | 내용 | 소유 | 위치 |
|---|---|---|---|
| 기준 | rules, schemas, skills, tools, golden, 매니페스트 | 사람만 수정, git | 저장소 `workgroups/*`, `rules/`, `schemas/` |
| 커널 | 컴파일, 승인, 수집, 게이트, 판정, 기록, 평가, 관찰 | 사람이 작성, 기계가 실행 | 저장소 `kernel/` (예정) |
| 실행기 | 팩을 받아 out/ 에 산출물 | AI, 컨테이너 안 | 저장소 밖 |
| 기록 | runs, 점수, 실패 카드, 후보 | 기계가 기록, 사람이 읽음 | 저장소 `runs/`, `golden/` |

### 전체 흐름

```text
[기준층]  rules/ schemas/ skills/ tools/ golden/ workgroup.yaml   ← 사람만, git 커밋으로만
              │ 읽기 (커밋 해시 고정)                 │ 같은 해시로 검증
              ▼                                        ▼
카드 → 컴파일 → 팩 → 승인 ─ADMIT→ ▣ 실행기 ─out/→ 번들 → 게이트 → 판정 → 기록
      └── 시작 구조 (커널) ──┘  (컨테이너)   └──── 끝 구조 (커널) ────┘
                 ▲                                              │ FAIL · 재시도 남음
                 └──── rev+1 팩. feedback.txt 한 줄만 달라짐 ────┘
                                                                │ 기록 닫힘
                        비동기 · 사람 속도:  평가 → 관찰·집계 → 후보 → 사람이 기준 고침 → [기준층]
```

### 두 개의 시간 축
- 가운데 줄은 카드 한 장의 **동기 흐름**. 커널이 컴파일부터 기록까지 멈추지 않고 돌린다. 사람은 카드를 쓸 때와 HELD 가 날 때만 등장한다
- 아래 띠는 기준을 고치는 **비동기 흐름**. 어떤 카드의 수용도 막지 않고 실행기에 아무것도 되돌려주지 않는다. 되돌아가는 곳은 기준층 하나, 되돌리는 손은 사람

### 기준층은 두 곳에서만 읽힌다
컴파일이 기준을 읽어 팩에 넣고, 게이트가 같은 커밋의 기준으로 검사한다. 실행기는 원본을 직접 읽지 않는다. 이 분리가 있어야 "실행 중 기준이 바뀌지 않았다"를 해시 비교 하나로 확인할 수 있고, 실행기가 기준 파일을 고치는 일이 구조적으로 불가능해진다.

---

## 5. 시작 구조: 카드, 컴파일, 팩, 승인

목적: 기준을 "읽으라"고 말하는 대신, **읽을 것만 남긴 세계**를 실행기에 건넨다.

### 사전 통제의 두 종류 (D10)

| 종류 | 무엇 | 어떻게 | 강제되는가 |
|---|---|---|---|
| 세계 구성 | 읽기·쓰기·실행 가능한 것 | 팩 내용, 컨테이너 마운트, PATH 의 셔틀 | 예. 실행기 의지와 무관 |
| 판단 지시 | 어떻게 판단할 것인가 | rules 문장 | 아니오. 껍질은 게이트로, 핵은 골든으로 |

규칙 사다리 (위가 강함):

| 단계 | 뜻 | 수단 | email-triage 예 |
|---|---|---|---|
| 불가능 | 할 수 없다 | 팩, PATH, 컨테이너 | 링크 열기, 메일 발송, 기준 수정, private 읽기 |
| 형태 강제 | 그 모양이 아니면 받지 않는다 | 스키마 enum, 필수 필드 | 라벨 7개 중 하나, evidence 는 필드 목록에서만, reason 200자 |
| 논리 검사 | 산출물 위에서 기계 확인 | invariant | 확신 0.6 미만이면 needs_human, 개인 도메인은 unprocessed |
| 사후 측정 | 골든셋으로 잰다 | 골든 케이스, rubric | 도메인으로 판단했는가 (g002) |
| 참조 | 읽고 따르길 기대 | rules 문장 | 스레드는 직전 라벨을 이어받되 새 요구면 바꾼다 |

### 카드 (사람)
- **역할**: 작업 하나의 의도. 무엇을, 어떤 입력으로, 어떤 조건에서. 어떻게는 적지 않는다
- **출력**: `card.md`. frontmatter + 지시문 한 줄. `schemas/task-card.schema.json`
- **책임**: 워킹그룹, 입력 위치, must (1~5개, 게이트가 확인 가능한 문장), 예산 상한
- **책임 아님**: 절차, 판단 규칙, 산출물 형식. 카드 작성 10분 초과 또는 같은 must 세 번 반복 → 기준으로 승격
- **실패하면**: 컴파일이 스키마 오류로 멈춤. 실행기 미시작

```markdown
---
id: 2026-09-02-inbox-triage
workgroup: email-triage
input: mail://work/inbox?since=2026-09-01
max_retries: 1
must:
  - 읽기만 한다. 회신, 전달, 삭제, 링크 열기 호출 금지
  - rules/classification.md 에 없는 라벨은 unknown 으로 낸다
---
어제 이후 받은 메일을 rules/classification.md 기준으로 분류해 result.json 으로 낸다.
```

사람이 손으로 쓰는 것은 지시문, must, input 이다. 카드가 짧은 것은 설계다.

### 컴파일 (커널)
- **역할**: 카드와 매니페스트로 팩을 만든다. 판단 없음
- **입력**: `card.md`, `workgroups/<name>/workgroup.yaml`, 기준층 현재 커밋
- **출력**: `runs/<id>/pack/` 과 해시
- **책임**:
  - 결정론: 같은 커밋 + 같은 카드 = 같은 팩 해시. 다르면 컴파일이 버그
  - 맥락 복사: 매니페스트 `context` 의 rules, SKILL 을 `pack/context/` 로. 원본 경로는 팩에 없음
  - 계약 스냅샷: 스키마와 게이트 목록을 `pack/contract/` 로. 실행 중 매니페스트가 바뀌어도 이 실행은 컴파일 시점 계약으로 검사
  - 입력 물질화: `inputs.fetch` 를 셔틀로 실행해 `pack/input/` 생성. `input-ids.txt` 도 여기서. 실행기는 입력을 다시 가져오지 않는다
  - 권한 서술: `permissions.json` (실행기 중립). 컨테이너에서는 마운트 지시로 번역
  - 셔틀 생성: 허용 도구마다 `pack/bin/<tool>`
  - 출처 기록: `pack.json` 에 커밋 해시, 팩 해시, rev, 예산, 시각
- **책임 아님**: 입력 내용 판단, 카드 타당성, 실행기 선택
- **실패하면**: `COMPILE_ERROR`. 기준층 문제이므로 실패 카드가 아니라 사람에게 바로 보고

### 팩 (데이터)
실행기가 볼 수 있는 세계의 전부. 만들어진 뒤 바뀌지 않는다. 바꿔야 하면 새 rev.

```text
runs/<id>/pack/
  pack.json                      id, workgroup, workgroup_commit, pack_hash, rev, budget, review, created_at
  card.md
  context/                       rules/*.md, SKILL.md 복사본 (읽기 전용)
  contract/result.schema.json    산출물 계약. 실행기도 이것을 보고 쓴다
  contract/gate.yaml             이 실행에 적용될 검사 목록 스냅샷
  permissions.json               read / write / exec 패턴
  input/input.json               물질화된 입력
  input/input-ids.txt            완결성 검사 기준 목록
  bin/                           도구 셔틀. PATH 에 이것만 올라간다
  feedback.txt                   rev ≥ 2 에서만. 이전 판정 한 줄
```

- **왜 불변인가**: 게이트의 해시 비교, 재시도의 "feedback 한 줄뿐" 확인, 실행기 간 비교. 셋 다 팩 불변에 기댄다
- **rev**: `rev+1` 팩은 `feedback.txt` 외 모든 파일 해시가 이전 rev 와 동일해야 한다

### 승인 (커널)
- **역할**: 팩이 완전한지 확인하고 실행을 허가
- **입력**: 팩, 전역 정책 (`rules/paths.md`, 예산 상한)
- **출력**: `ADMIT` 또는 `REJECT <reason>`. ADMIT 시 `review` 플래그 확정
- **확인**: 카드 스키마와 지시문 3줄 이내 / context·contract·bin 파일 존재 / 입력 물질화, ids 비어 있지 않음 / 예산 상한 이내 / rev ≥ 2 면 feedback 외 해시 불변 / 경로 규칙 → review
- **책임 아님**: 산출물, must 준수 여부 (게이트의 일)
- **실패하면**: `REJECTED`. 재시도 대상 아님. 카드나 기준을 고쳐 새 카드로

승인과 게이트를 나누는 이유: 승인은 "시작해도 되는가", 게이트는 "결과를 받아도 되는가". 합치면 실행기가 돌아간 뒤에야 입력이 비어 있었음을 알게 된다.

---

## 6. 실행기 경계: 컨테이너, 어댑터, 셔틀

```text
run(pack_dir, out_dir, budget) -> exit_code
```

### 컨테이너 (D11)
컨테이너 안에 있는 것:
- `pack/` 읽기 전용 마운트
- `out/` 쓰기 마운트
- `pack/bin` 의 셔틀. PATH 는 이것만
- 모델 API 엔드포인트로 나가는 네트워크 (허용 목록 프록시)

컨테이너 안에 없는 것:
- 기준층 원본, 다른 프로젝트, 홈 디렉터리, 과거 세션
- 메일 서버 토큰, 레포 쓰기 권한 등 도구 자격증명. 호스트 쪽 도구가 갖는다
- 셔틀 외의 실행 파일

개발 워킹그룹은 레포 사본(worktree)을 팩 안에 마운트한다. 게이트의 `test` 검사도 컨테이너 안에서 같은 사본으로 돈다.

### 셔틀 = 자격증명 경계
셔틀은 컨테이너 안의 얇은 실행 파일이다. 호출을 `out/tool.log` 에 `ts tool args exit` 한 줄로 기록하고, 호스트의 실제 도구를 소켓으로 호출한다. 실제 도구가 인증하고 결과만 돌려준다. 실행기는 tool.log 에 쓸 권한이 없다.

### 어댑터
`permissions.json` 과 팩을 실행기 형식으로 번역해 컨테이너 안에서 실행기를 호출한다. 그 외 없음.
- claude -p: `.claude/settings.json` allow/deny, `--system-prompt-file pack/context`, cwd = pack
- codex: sandbox 설정, cwd = pack
- hermes: `-s -t`, `external_dirs = pack/context`, cwd = pack

컨테이너가 있으면 어댑터의 권한 번역은 이중 안전장치가 된다. 없어도 게이트의 frozen·paths 가 사후 검증한다.

### 예산과 관찰
시간이나 턴을 넘기면 커널이 컨테이너를 종료한다. 그때까지의 out/ 은 그대로 수집되어 게이트로 간다. stdout, stderr, 종료 코드, 소요 시간은 커널이 밖에서 캡처한다. 비정상 종료도 정상 종료와 같은 길을 간다.

---

## 7. 끝 구조: 수집, 게이트, 판정, 기록

목적: 실행기가 무엇을 주장하든, **남은 파일과 커널이 본 로그만으로** 받을지 정한다.

### 수집 (커널)
- out/ → bundle/ 복사, 파일별 해시 `manifest.json`, `meta.json` (exit, ms, stdout tail, tokens)
- 왜: 게이트가 out/ 을 직접 읽으면 검사 중 변경 가능성을 배제할 수 없다. "게이트가 본 것 = 평가가 본 것 = HELD 에서 사람이 본 것"이 스냅샷으로 성립
- 책임 아님: 내용 해석

### 게이트 (커널)
- `pack/contract/gate.yaml` 검사를 순서대로. 각 검사는 `(pack, bundle) → PASS | FAIL` 순수 함수. 첫 실패에서 멈춤
- 원칙: LLM 안 부름 / `claims` 안 읽음 / 도구 로그는 셔틀이 쓴 것만 / 같은 입력 = 같은 판정
- 책임 아님: 품질 판단. 넣기 시작하면 게이트가 느려지고 흔들리며 실행기는 게이트를 넘는 쪽으로 수렴

| 검사 | 보는 것 | 실패 예 |
|---|---|---|
| schema | bundle 파일이 contract 스키마에 맞는가 | `FAIL schema: $.items[2].label not in enum` |
| completeness | input-ids 가 지정 필드에 정확히 한 번씩 | `FAIL completeness: missing=[g004]` |
| tool_log | 호출이 allow 목록 안인가 | `FAIL tool_log: send-mail not allowed` |
| invariant | 산출물 위 논리식 | `FAIL invariant: needs_human required for g004` |
| frozen | 기준층 현재 해시 = pack.json 의 workgroup_commit | `FAIL frozen: rules/classification.md changed` |
| test | 코드 워킹그룹. 지정 명령 종료 코드 | `FAIL test: pnpm test exit 1` |
| paths | bundle 밖 변경 없음, forbidden_paths 미접촉 | `FAIL paths: src/auth/token.ts modified` |

### 판정 (커널)
- PASS, review 없음 → `ACCEPTED`
- PASS, review 있음 (경로 규칙 / 카드 `review: required` / 재시도 후 통과) → `HELD`. 사람이 diff 승인
- FAIL, 재시도 남음 → 컴파일에 rev+1 요청. feedback.txt 한 줄만 달라짐
- FAIL, 소진 → `FAILED_OUT`. 실패 카드. 기준은 건드리지 않음
- 책임 아님: 재시도 전략 변경, 예산 연장, 게이트 완화

재시도 후 통과를 HELD 로 보내는 이유: 두 번째 시도는 "맞게 고친 것"과 "게이트를 넘게 고친 것"을 구분할 수 없다. 사람이 한 번 보고, 반복되면 관찰이 후보로 올린다.

### 기록 (커널)
- `run.json` 한 번 쓰고 닫음. 인덱스 한 줄
- 담는 것: 최종 상태, 시도별 verdict, rev 별 팩 해시, 번들 해시, review 원인, 실패 카드 경로, 시간·토큰
- 불변. HELD 승인은 `review.json`, 평가는 `score.json` 을 옆에 추가

---

## 8. 상태 기계

```text
CREATED → COMPILED → ADMITTED → RUNNING → COLLECTED → PASSED → ACCEPTED ─(비동기)→ EVALUATED
                   ↘ REJECTED                       ↘ FAILED         ↘ HELD ─사람─→ ACCEPTED
                                                       ├─ attempts<max → COMPILED (rev+1)
                                                       └─ 소진 → FAILED_OUT
```

| 상태 | 뜻 | 다음으로 보내는 이 |
|---|---|---|
| CREATED | 카드 접수 | 커널 |
| COMPILED | 팩 생성, 해시 확정 | 커널 |
| ADMITTED / REJECTED | 실행 허가 / 불허(종단) | 커널 |
| RUNNING | 실행기 동작 중. 유일하게 실행기가 소유 | 실행기 종료 또는 예산 초과 |
| COLLECTED | 번들 고정 | 커널 |
| PASSED / FAILED | 게이트 판정 | 커널 |
| ACCEPTED | 수용(종단). 평가 대상 | 평가 프로세스 |
| HELD | 사람 승인 대기 | 사람 → ACCEPTED / REJECTED_BY_REVIEW |
| FAILED_OUT | 재시도 소진(종단). 실패 카드 | 주간 루틴에서 사람이 분류 |
| EVALUATED | score.json 존재 | 관찰 집계 |

---

## 9. 평가와 갱신

수용이 끝난 뒤에 돈다. 수용을 막지 않고, 실행기에 아무것도 되돌려주지 않는다.

### 평가 (커널 + 사람)
- 수용된 번들을 골든 케이스와 대조. 게이트가 아님
- 기계 채점: 라벨 일치, 근거 포함, 확신·정확도 일치 → `score.json`
- 판정 채점: 정답이 하나가 아닌 것. 사람이 rubric 으로, 또는 작업 세션과 분리된 판정 세션이 rubric 만 보고. 참고값
- 책임 아님: 수용 여부, 기준 수정

rubric 예 (email-triage, 케이스당 10점):

| 항목 | 만점 | 기준 |
|---|---|---|
| 라벨 정확 | 4 | expected 일치 4, acceptable 2, 그 외 0 |
| 근거 타당 | 2 | must_cite 포함 2, 실제 필드지만 다른 것 1, 추측 0 |
| 확신 보정 | 2 | 맞으면 높고 틀리면 낮았나. 틀렸는데 0.8 이상이면 0 |
| 절제 | 2 | 애매하면 unknown, 명확하면 단정. 남발·회피 모두 감점 |

### 관찰 (커널)
- 인덱스 위 집계: 실패 분류 빈도, 재시도율, 점수 추세·편차, 반복 must, HELD 거부 건, `enforce: reference` 규칙의 골든 실패
- 임계: 같은 신호 3회 → `candidates.md` 한 줄. AI 가 요약을 돕는 마지막 지점
- 책임 아님: 기준 수정. 커널에 기준층 쓰기 코드 없음

### 갱신 (사람, 주 단위)
1. runs 의 지난 주 기록. HELD 와 재시도 후 통과 먼저
2. 실패 카드에 분류(실행 문제 / 기준 모호 / 도구 문제)와 후보 여부
3. 채점 대기열의 판정 항목
4. candidates 에 결정 기입
5. 기준 파일 수정, 커밋 메시지에 관찰 출처
6. runs 실제 건을 익명화해 골든 승격, note 에 시험하는 규칙

### 판단의 핵을 다루는 세 겹 (D12)
판단은 남는다. 남은 판단에 대해 하는 일은 통제가 아니라 이것이다.

1. **틀려도 싸게**: `confidence`, `needs_human`, `unknown` 으로 불확실을 위로 올린다. 모르는데 모른다고 한 것은 사람의 1분. 틀렸는데 확신한 것은 골든 "보정" 0점
2. **핵마다 골든 하나**: g001 정상 경로, g002 표시 이름에 속는가, g003 이어받기만 하는가, g004 모르는 걸 모른다고 하는가
3. **반복되면 사다리 위로**: 껍질을 넓힌다(도메인 목록 + check-domain 셔틀 + invariant) → 라우팅을 조인다(그 유형은 needs_human 강제) → 예시를 준다. "더 신중하게"는 추가하지 않는다

하지 않는 것: 실패했다고 게이트 완화, 실행기 프롬프트 손질. 같은 케이스 3회에서 점수 편차가 크면 rubric 이나 rules 가 모호한 것.

비유: 신입 직원에게 메일 분류를 맡기는 것과 같다. 매뉴얼을 준다(rules), 권한을 뺏는다(컨테이너), 근거를 적게 한다(evidence), 모르면 물어보라 하고 벌하지 않는다(unknown), 샘플을 감사한다(golden), 감사로 매뉴얼을 고친다(갱신). 머릿속은 통제하지 않는다.

---

## 10. 시나리오 세 개 (email-triage, 골든 4건)

### A. 정상 통과
1. 사람: 카드 작성
2. 컴파일: 커밋 027626b 에서 rules 2, SKILL, 스키마, gate.yaml 복사. fetch-mail 셔틀로 입력 4건. ids g001..g004. rev 1, review false
3. 승인: ADMIT
4. 실행기(컨테이너): 4건 분류, out/result.json. 도구 호출 0. exit 0, 41초
5. 수집: bundle, manifest, meta
6. 게이트: schema → completeness → tool_log → invariant → frozen 모두 PASS
7. 판정: ACCEPTED
8. 기록: run.json 닫힘
9. 평가(비동기): 기계 31/32. 절제 항목은 채점 대기열

### B. 게이트 실패, 재시도 후 통과, 사람 확인
1. 실행기 rev 1: g004 unknown, conf 0.45, needs_human false
2. 게이트: `FAIL invariant: needs_human required for g004`
3. 판정: rev 2 요청
4. 컴파일 rev 2: 해시 동일, feedback.txt 한 줄
5. 승인: rev 검사 통과, ADMIT
6. 실행기: g004 needs_human true
7. 게이트: PASS
8. 판정: retries=1 → HELD
9. 사람: rev1/rev2 diff 한 줄 확인, 승인 → ACCEPTED, review.json
10. 관찰: invariant/needs_human 1회. 임계 미달. 3회면 "판단 순서 4를 SKILL 3단계로" 후보

### C. 재시도 소진, 실패 카드, 기준 갱신
1. 실행기 rev 1: 개인 도메인 메일을 처리도 안 하고 unprocessed 에도 안 넣음. 3건
2. 게이트: `FAIL completeness: missing=[g005]`
3. 판정: rev 2
4. 실행기 rev 2: g005 를 items 에 fyi 로. 규칙은 여전히 안 지킴
5. 게이트: completeness PASS → `FAIL invariant: personal-domain g005 must be in unprocessed`
6. 판정: 소진 → FAILED_OUT, 실패 카드 생성
7. 사람(주간): 분류 "기준 모호". boundaries 가 "처리하지 않는다"고만 했고 SKILL 에 unprocessed 문장 없음
8. 관찰: 같은 분류 3건 → candidates 한 줄
9. 사람: boundaries.md, SKILL.md 수정. g005 익명화해 골든 005 승격. 커밋

세 시나리오에서 사람은 카드 작성, HELD 승인, 주간 갱신 세 곳에만 등장하고 실행기에 직접 말하는 자리는 없다.

---

## 11. 현재 구현 상태와 거리

### 저장소 (`~/dev/chun-su`, main, 커밋 027626b 기준)
- `docs/00~08` 기준 문서. 이 통합본이 상위
- `schemas/` 작업 카드, 공통 산출물, 실행 기록 스키마
- `rules/` 전역 경계, 경로 규칙
- `runner/` bash 러너, 입구 게이트, 스키마 검증기, 기계 채점기. 커널로 대체 예정
- `workgroups/_template`, `workgroups/email-triage` (규칙, 스키마, 스킬, fetch 도구, 출구 게이트, 골든 4건, rubric, 예시 카드)
- `golden/failures/_template.md`, `golden/candidates.md`
- `runs/2026-09-02-golden-smoke/` 실행 기록 예시
- 검증 완료: 입구 게이트 거부/통과, 출구 게이트 위반 7종 각각 다른 검사명으로 실패, 러너 수용·거부·재시도 경로, 기록 스키마 일치

### 거리

| 단계 | 지금 | 바뀌는 것 |
|---|---|---|
| 카드 | 있음 | 그대로. gate, forbidden_paths 필드는 매니페스트로 |
| 컴파일 | 없음. 카드에 "읽어라" | 매니페스트 기반 팩 생성, 해시, rev, 입력 물질화, 셔틀 |
| 승인 | gate-in.sh, 카드만 | 팩 완전성, rev 검사, review 플래그 |
| 실행 | EXECUTOR 문자열, 호스트에서 직접 | 컨테이너 + 어댑터 계약 |
| 도구 | fetch-mail.sh 가 스스로 로그 | 커널 셔틀이 감싸서 기록. 자격증명은 호스트 |
| 수집 | 없음 | 스냅샷 + 해시 + meta |
| 게이트 | 워킹그룹별 gate-out.sh, 6개 하드코딩 | gate.yaml 선언, 커널이 7종 해석 |
| 판정·기록 | run-task.sh 에 섞임, HELD 없음 | 상태 기계 명시, HELD·review.json |
| 평가 | score.py stdout | score.json + 채점 대기열 |
| 관찰 | candidates.md 수동 | 집계 자동, 임계 3회, 결정은 사람 |
| 규칙 | 문장만 | `enforce:` 태그로 사다리 위치 표시 |

---

## 12. 계약 목록과 매니페스트

"구조를 정확하게 핸들링할 수단"은 이 목록이다. 커널은 이 계약들 사이를 옮기는 코드이고 그 이상이 아니다.

| 계약 | 형식 | 생산자 → 소비자 | 지금 |
|---|---|---|---|
| 작업 카드 | md + task-card.schema.json | 사람 → 컴파일 | 있음 |
| 워킹그룹 매니페스트 | workgroup.yaml | 사람 → 컴파일, 게이트, 평가 | 신설 |
| 팩 | 디렉터리 + pack.json | 컴파일 → 승인, 어댑터, 게이트, 평가 | 신설 |
| 어댑터 | run(pack, out, budget) → exit | 커널 → 실행기 | EXECUTOR 문자열. 승격 |
| 도구 셔틀 | pack/bin/*, `ts tool args exit` | 커널 → 게이트 | 도구 자체 기록. 이동 |
| 번들 | 디렉터리 + manifest.json + meta.json | 수집 → 게이트, 평가, HELD | 신설 |
| 게이트 선언 | gate.yaml, 순서 있음 | 사람 → 게이트 | bash. 선언으로 |
| 판정 | PASS / FAIL check: msg | 게이트 → 판정, feedback | 있음 |
| 실행 기록 | run.json | 기록 → 평가, 관찰 | 있음. 상태·해시 추가 |
| 점수 | score.json + 채점 대기열 | 평가 → 관찰 | stdout. 저장으로 |
| 후보 | candidates.md append | 관찰 → 사람 | 있음. 집계 자동화 |

### 매니페스트 예시
새로 생기는 유일한 사람 작성 파일. 컴파일, 게이트, 평가가 모두 이것만 읽는다.

```yaml
name: email-triage
inputs:
  fetch: tools/fetch-mail.sh          # 컴파일이 셔틀로 실행해 pack/input 생성 (호스트에서, 자격증명 보유)
context:                              # pack/context 에 복사
  rules: [rules/classification.md, rules/boundaries.md]
  skill: skills/email-triage/SKILL.md
  schema: schemas/result.schema.json
permissions:                          # 컨테이너 마운트 / 어댑터 권한으로 번역
  read:  [pack/**]
  write: [out/**]
  exec:  [fetch-mail]                 # 이것만 pack/bin 에 셔틀 생성
budget: {turns: 60, seconds: 900, retries: 1}
gate:                                 # 순서대로, 첫 실패에서 중단
  - schema:       {file: result.json, schema: contract/result.schema.json}
  - completeness: {ids: input/input-ids.txt, fields: [items[].message_id, unprocessed[].message_id]}
  - tool_log:     {allow: [fetch-mail]}
  - invariant:    {name: needs_human, expr: "all(i.needs_human for i in items if i.confidence < 0.6 or i.label == 'suspicious')"}
  - frozen:       {paths: [rules, schemas, skills, golden]}
eval:
  cases: golden/cases
  rubric: golden/rubric.md
  machine: [label, must_cite, calibration]
```

---

## 13. 불변식과 강제 수단

각 줄의 오른쪽이 없으면 왼쪽은 희망 사항이다.

| # | 불변식 | 강제 수단 | 우회 시 잡히는 곳 |
|---|---|---|---|
| 1 | 실행기는 팩 밖을 보지 못한다 | 컨테이너에 팩만 마운트. 기준 원본 경로가 안에 없음 | 물리적 (컨테이너) |
| 2 | 실행기는 out/ 에만 쓴다 | 쓰기 마운트가 out/ 만 + 게이트 frozen·paths (이중) | 컨테이너, 게이트 |
| 3 | 기준은 실행 중 불변 | 컴파일 시 커밋 해시 고정, 게이트가 같은 해시 확인 | 게이트 frozen |
| 4 | 자기보고는 증거 아님 | 게이트 입력은 pack, bundle 만. claims 미사용. tool.log 는 셔틀이 씀 | 구조적 |
| 5 | 재시도 피드백은 한 줄 | rev+1 에서 달라지는 파일은 feedback.txt 하나. 승인이 확인 | 승인 |
| 6 | 기준은 사람 커밋으로만 | 커널에 기준층 쓰기 코드 없음. candidates append 만 | 코드 리뷰, git |
| 7 | 평가는 수용을 막지 않음 | run.json 닫힌 뒤 별도 프로세스 | 구조적 |
| 8 | 같은 팩 = 같은 판정 | 검사는 순수 함수. LLM·네트워크·시각 없음 | 게이트 재실행 비교 |
| 9 | 사람은 diff 를 본다 | HELD 는 해시 매니페스트 + 경로 규칙 파일만 제시 | HELD 화면 |
| 10 | 실행기는 입력을 다시 가져오지 않는다 | 컴파일이 물질화. 셔틀 호출은 tool.log 에 남음 | tool_log, 관찰 |
| 11 | 실행기는 자격증명을 갖지 않는다 | 토큰은 호스트 도구가 보유. 셔틀은 소켓으로 호출만 | 물리적 (컨테이너) |

남는 약점: 모델 API 로 나가는 네트워크. 허용 목록 프록시로 닫는다.

---

## 14. 열린 결정과 검토 질문

### 결정됨 (이 세션에서)
- 팩 격리 수준 → 컨테이너 (D11)
- 셔틀 범위 → 컨테이너 안 PATH 전체가 셔틀. 셔틀 없는 명령은 존재하지 않음 (D11)
- 판단 지시의 처리 → 껍질·핵 분해, 라우팅, 골든, 사다리 (D10, D12)

### 정해야 할 것
1. **매니페스트의 범위**: invariant 표현식을 어디까지. 제안: 필드 비교와 all/any 까지, 그 이상은 검사 종류 추가
2. **HELD 의 화면**: diff 와 경로 목록 / 번들 전체. 세션 로그는 보이지 않아야 함
3. **평가의 트리거**: 기록 직후 자동 / 주간 배치. 사람 속도라면 배치
4. **컴파일 오류의 처리**: 실패 카드 / 별도 채널로 사람에게
5. **네트워크 프록시**: 컨테이너 egress 허용 목록을 어디서 관리하나. 매니페스트인가 전역 정책인가
6. **개발 워킹그룹의 팩**: 레포 사본을 팩에 넣는 방식. worktree 마운트인가 clone 인가. 게이트 test 가 컨테이너 안에서 돌 때 의존성 캐시는 어디에

### 검토자가 던져볼 질문
- 컨테이너 밖으로 나갈 수 있는 길이 모델 API 외에 정말 없는가. DNS, 시각 동기화 같은 부수 채널은
- 재시도에서 feedback 한 줄이 충분한가. 더 넘겨도 "게이트를 넘게 고치는" 수렴을 피할 수 있는가
- HELD 가 잦으면 사람이 다시 병목. 경로 규칙과 "재시도 후 통과 → HELD" 중 어느 것을 먼저 풀 것인가
- 게이트 7종으로 email-triage 와 개발 워킹그룹을 모두 덮는가
- 임계 3회는 적절한가. 주간 건수가 적으면 한 달이 걸릴 수 있다
- 매니페스트가 사람이 쓰는 두 번째 파일이 되었다. 카드와의 경계는 명확한가
- 규칙 사다리에서 "참조"로 남은 규칙이 몇 개인가. 그 수가 늘면 어떻게 아는가

---

## 15. 다음 단계

1. 이 문서와 `docs/08` 커밋
2. email-triage 규칙 파일에 `enforce:` 태그 적용 (부록 B 를 규칙 파일로 옮김)
3. 커널 최소 구현: 컴파일, 승인, 컨테이너 어댑터 1개(claude -p), 선언형 게이트, 판정·기록. 러너 대체
4. email-triage 카드 1건을 컨테이너에서 끝까지 돌려 시나리오 A 재현
5. 시나리오 B, C 를 의도적으로 재현해 HELD 와 실패 카드 경로 확인
6. 주간 루틴 1회. 규칙 한 줄 고치고 커밋
7. 그 뒤 개발 워킹그룹: 레포 마운트, test 게이트, 경로 규칙

---

## 부록 A. 용어

| 용어 | 뜻 |
|---|---|
| 기준층 | 사람이 소유하고 git 커밋으로만 바뀌는 파일 집합 |
| 워킹그룹 | 작업 영역 하나의 기준, 게이트 선언, 골든셋을 한 폴더에 모은 단위 |
| 매니페스트 | `workgroup.yaml`. 무엇을 읽고, 낼 수 있고, 검사받는지 선언 |
| 카드 | 사람이 쓰는 유일한 입력. 지시 한 줄과 must 몇 줄 |
| 커널 | 단계 사이를 옮기는 작은 프로그램. 판단하지 않고 계약만 확인 |
| 컴파일 | 매니페스트와 카드로 팩을 만드는 결정론적 단계 |
| 팩 | 실행기가 볼 수 있는 세계의 전부. 불변, 해시. 재시도는 rev |
| 승인 | 팩 완전성 확인. ADMIT / REJECT |
| 실행기 | 팩을 받아 out/ 에 산출물을 내는 블랙박스. 컨테이너 안 |
| 어댑터 | 팩을 특정 실행기 형식으로 번역해 호출하는 실행기별 코드 |
| 셔틀 | 컨테이너 안의 도구 래퍼. 한 줄 기록 후 호스트 도구를 호출. 자격증명 경계 |
| 번들 | out/ 의 스냅샷 + 해시. 이후 단계는 번들만 본다 |
| 게이트 | 선언된 검사를 순서대로. PASS / FAIL |
| 판정 | 수용, 보류, 재시도, 실패를 가르는 단계 |
| HELD | 게이트 통과했으나 사람 승인 필요 |
| 실패 카드 | 재시도 소진 작업의 기록. 기준은 여기서 고치지 않음 |
| 골든 케이스 | 입력, 기대 결과, 필수 근거. 규칙 하나를 시험 |
| 후보 | 반복 관찰이 올라가는 기준 수정 제안. 결정은 사람 |
| 껍질 / 핵 | 규칙의 기계 확인 가능한 부분 / 남는 판단 부분 |
| 사다리 | 규칙의 강제 등급. 불가능 > 형태 > 논리 > 측정 > 참조 |

## 부록 B. email-triage 규칙의 껍질·핵 분해

| 규칙 | 껍질 (어디서 강제) | 핵 (어떻게 측정) | enforce 태그 |
|---|---|---|---|
| 1 발신자는 도메인으로 본다 | evidence 에 sender_domain 필수 (스키마) | 흉내 도메인인가 (골든 g002, 보정) | schema + eval |
| 2 스레드는 이어받되 새 요구면 바꾼다 | 이력 있으면 evidence 에 thread_history (invariant) | 새 요구인가 (골든 g003) | invariant + eval |
| 3 확신 0.6 미만은 unknown | conf < 0.6 → label unknown (invariant) | 확신을 얼마로 매기나 (골든 g004, 보정) | invariant + eval |
| 4 조건 시 needs_human | 조건 → true (invariant) | 없음 | invariant |
| 5 근거는 실제 필드에서만 | evidence enum (스키마) | 정말 보고 판단했나 (골든 must_cite) | schema + eval |
| 경계: 링크·첨부 열지 않음 | 컨테이너에 브라우저 없음 | 없음 | capability |
| 경계: 발송·삭제 금지 | PATH 에 없음 | 없음 | capability |
| 경계: 개인 도메인은 unprocessed | invariant | 없음 | invariant |
| 경계: 원문 미기록 | reason maxLength 200 (스키마) | 요약이 원문 복사인가 (골든) | schema + eval |

순수 참조로 남는 규칙: 없음. 핵이 남는 규칙: 1, 2, 3, 5. 이것이 골든 케이스가 시험하는 것이다.

## 부록 C. Hermes 설치 기록 (참고, 보류 상태)

- Hermes Agent v0.21.0 (2026-08-31), `--skip-setup --no-skills` 로 설치. 코드 `~/.hermes/hermes-agent/`, 데이터 `~/.hermes/`
- 인증: 이 머신의 Claude Code 로그인을 재사용. `hermes auth status anthropic` = logged in
- 설정: 모델 `claude-sonnet-5`, approvals manual / 비대화형 deny, CLI 도구셋 terminal·file·skills·todo, 스킬 자동 생성·curator 끔, 스킬은 `external_dirs` 로 읽기 전용, 메모리 자동 기록 끔, 텔레메트리 끔, 게이트웨이·크론 미설정
- 단발 호출 검증: `hermes chat --oneshot -q ...` 정상 응답
- 판단: Hermes 의 권장 사용법(자기진화, 게이트웨이)과 반대 구성. 게이트웨이 필요 시점까지 보류. 상세는 `docs/06-hermes-setup.md`
