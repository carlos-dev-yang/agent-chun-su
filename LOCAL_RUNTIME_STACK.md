# Chun-su 로컬 개발·운영 구성

작성일: 2026-09-05. 상태: 사용자가 Go + SQLite를 구현 기반으로 확정했다. 세부 라이브러리·실행기·연동 계약은 아래 추천안을 바탕으로 단계별 검토한다. 최신 작업 순서는 [영문 실행계획](IMPLEMENTATION_PLAN.md)에 정리했다. 구현·패키지 설치·DB 생성·서비스 등록은 아직 수행하지 않았다. 아래 명령과 디렉터리는 만들 제품의 설계이며 현재 제공되는 기능이 아니다.

**Go + 내장 SQLite를 사용하며, CLI 라이브러리는 Cobra를 권한다.** 한 실행 파일이 CLI, 작업 준비, 영속 큐, 실행 제어, 결과·평가 기록을 담당한다. 처음에는 터미널에서 필요할 때 실행하고, 정기 운영을 시작할 때 같은 실행 파일을 macOS의 사용자용 `launchd` 서비스로 등록한다.

운영자가 관리할 것은 **Chun-su 실행 파일, 업무 기준과 설정, 로컬 데이터, 선택한 AI 실행기와 계정 연결**이다. DB 서버나 별도 큐 서버의 설치는 초기 구성에 필요하지 않다. 실행기 자체의 설치·로그인·격리는 교체 가능한 별도 구성 요소다.

**1. 현재 Mac에서 확인한 환경**

| 항목 | 직접 확인한 상태 | 이번 선택에 주는 의미 |
|---|---|---|
| 운영체제·CPU | macOS 계열 Darwin 25.5.0, arm64 | 첫 지원 대상은 현재 Apple Silicon Mac으로 정한다. |
| Go | 1.26.4, Homebrew 설치본 | Go 설치 여부가 착수 장애물은 아니다. |
| Node | 25.2.1, Homebrew 설치본 | 사용할 수는 있으나 새 운영 기반에는 지원되는 LTS 계열을 선택해야 한다. |
| Python | Homebrew의 Python 3.14 경로 | 다른 스크립트 언어도 가능하지만 이미 설치된 Go를 피해야 할 이유는 없다. |
| SQLite CLI | PATH에서는 Android SDK의 3.50.6이 선택됨. `/usr/bin/sqlite3`도 존재 | 앱은 PATH의 SQLite 명령을 호출하지 않고 자체 라이브러리를 포함한다. |
| macOS 서비스 도구 | `launchctl`과 로컬 설명서 확인 | 별도 프로세스 관리 프로그램의 설치 없이 사용자용 백그라운드 실행을 구성할 수 있다. |

현재 Node는 `/opt/homebrew/Cellar/node/...`에 있고 `/usr/bin/node`는 없었다. 이 Mac에서 사용할 수 있다는 사실과 모든 새 Mac에 기본 런타임으로 존재한다는 가정은 구분해야 한다. Go도 현재 Homebrew 설치본이다.

공식 문서 기준으로 Node 25는 지원 종료, Node 24는 LTS다. Go 1.26 계열은 지원 대상이며 확인 시점의 최신 패치는 1.26.8이다. 최초 개발 빌드에서는 지원 패치와 의존성 조합을 확인해 고정한다. 현재 설치된 버전을 최신이라고 가정하지 않으며 이번 작업에서 업데이트하지 않았다. [Node 릴리스](https://nodejs.org/en/about/previous-releases), [Go 릴리스](https://go.dev/doc/devel/release)

**2. 다른 선택지와 Go를 권하는 이유**

| 선택 | 장점 | 설치·운영에서 고려할 점 | 이 프로젝트 판단 |
|---|---|---|---|
| Go | 컴파일한 실행 파일로 배포. 프로세스·시간 제한·동시 작업·HTTP·로그·SQL 접근을 표준 라이브러리 중심으로 구성 가능 | 개발 시 Go 도구와 모듈 다운로드가 필요. OS 기능을 연결하는 라이브러리의 빌드 조건 확인 필요 | 사용자는 실행 파일과 업무 자산을 사용하고 코어는 외부 실행기를 제어하는 제품에 적합 |
| Node + JavaScript/TypeScript | JS·TS 라이브러리를 활용하기 쉽고 같은 영속 큐 구조도 구현 가능 | 일반 배포는 지원되는 Node와 패키지 관리가 필요. 단일 실행 파일 배포를 선택하면 별도 패키징 조건 확인 | 필요한 실행기·SDK가 Node에 강하게 결합되거나 JS·TS 개발 편의가 더 중요해질 때 대안 |
| Python | SQLite 표준 모듈이 있고 평가·자료 처리 스크립트에 활용 가능 | Python 버전과 의존성 환경을 관리해야 함. 코드 배포와 실행 환경을 함께 관리 | 이후 특정 평가 도구가 필요할 때 추가 가능. 초기 상주 코어의 우선 선택은 아님 |
| Rust | Cargo로 실행 파일을 빌드하는 배포 방식 가능 | Rust 도구·의존성·개발 방식을 추가로 선택해야 함 | Go 대비 이번 요구에서 얻을 추가 이득이 아직 확인되지 않음 |

Node도 파이프라인·재시도·큐 제어를 구현할 수 있다. Go를 권하는 주된 이유는 터미널 중심의 사용 방식과 실행 파일 배포, 외부 실행기를 제어하는 코어의 역할이다. 현재 Go가 설치되어 있다는 사실은 당장의 착수 편의일 뿐, 다른 사람에게 널리 배포할 언어를 정하는 근거로 크게 평가하지 않는다. 언어별 메모리·CPU·속도 순위를 실제로 측정한 것은 아니다.

Go 공식 튜토리얼은 소스와 의존성을 컴파일해 실행 파일을 만들고 직접 실행하는 흐름을 설명한다. Python은 SQLite 모듈과 가상 환경을 제공하고, Rust는 Cargo 빌드로 실행 파일을 만든다. [Go 빌드](https://go.dev/doc/tutorial/compile-install), [Python SQLite](https://docs.python.org/3/library/sqlite3.html), [Python 환경 관리](https://docs.python.org/3/tutorial/venv.html), [Rust Cargo](https://doc.rust-lang.org/book/ch01-03-hello-cargo.html)

Node 24의 내장 `node:sqlite`도 후보가 될 수 있지만 확인한 문서의 안정성 표시는 release candidate이며 `DatabaseSync`는 동기 API다. 작은 로컬 큐에 사용할 수 없다는 뜻은 아니고, API 안정성과 긴 DB 작업의 실행 방식을 함께 고려해야 한다. Node에도 단일 실행 파일 기능이 있으나 현재 문서는 active development로 표시한다. 따라서 ‘Node는 반드시 별도 런타임이 있어야만 배포 가능하다’고 단정하지 않는다. [Node 24 SQLite](https://nodejs.org/download/release/latest-v24.x/docs/api/sqlite.html), [Node 단일 실행 파일](https://nodejs.org/api/single-executable-applications.html)

**2-1. Go와 Node의 배포·호환성 비교**

Go로 만들었다는 이유만으로 널리 배포하기 어려워지는 것은 아니다. 사용자가 완성된 프로그램을 설치하는 경우와 개발자가 코드를 가져다 확장하는 경우에 편리한 조건이 다르다. 여기서 Node 안은 유지보수를 고려한 Node + TypeScript 구성을 뜻한다.

| 비교 기준 | Go | Node + TypeScript |
|---|---|---|
| 완성된 제품의 사용자 설치 | 대상 OS·CPU용 실행 파일을 제공하면 사용자에게 Go 개발 도구가 필요하지 않음 | 일반적인 npm 배포는 호환되는 Node 런타임이 필요. 런타임을 포함한 실행 파일 배포도 가능 |
| 배포자에게 필요한 일 | 대상별 빌드·의존성 확인·패키징·업데이트 제공 | 지원 Node 버전·패키지 의존성 관리. 런타임을 포함하면 대상별 패키징·업데이트도 담당 |
| macOS·Windows·Linux 지원 | 주요 운영체제를 지원. 대상별 빌드와 실제 기능 확인 필요 | 주요 운영체제를 지원. 같은 JS 소스를 재사용하기 좋지만 런타임과 네이티브 모듈의 지원 조건 확인 필요 |
| AI SDK를 프로그램 안에서 직접 사용 | 해당 Go SDK가 있어야 편리. TypeScript 전용 SDK는 별도 연결이 필요 | TypeScript SDK를 직접 조합하고 해당 예제를 재사용하기 편리 |
| HTTP API·MCP·외부 CLI 연결 | 언어와 독립적인 계약으로 연결 가능 | 같은 방식으로 연결 가능 |
| 사용자가 지침·업무 기준을 수정 | Markdown·JSON 중심이면 Go를 배울 필요 없음 | Markdown·JSON 중심이면 TypeScript를 배울 필요 없음 |
| 개발자가 확장 코드를 작성 | 코어 수정은 Go 지식 필요. 별도 프로세스 확장은 다른 언어도 가능 | JS·TS에 익숙한 참여자가 npm 패키지와 확장 코드를 작성하기 편리 |
| SQLite·큐·재시도 | 내장 DB와 영속 상태로 구현 가능 | 같은 구조로 구현 가능. 언어가 내구성·복구를 자동으로 보장하지 않음 |
| 이후 웹 화면 추가 | 웹 화면을 별도로 만들고 Go 코어와 연결할 수 있음 | 화면과 서버의 TypeScript 타입·일부 코드를 공유하기 편리 |
| 상주 자원·실행 속도 | 실제 빌드와 운영 조건에서 측정 필요 | 실제 빌드와 운영 조건에서 측정 필요. 언어 이름만으로 업무 전체의 비용을 판단하지 않음 |

Go의 실행 파일 배포는 실제 여러 OS용 CLI에서 사용된다. GitHub CLI는 Go로 빌드하며 macOS·Windows·Linux에 설치 패키지와 실행 파일을 제공한다. 이 사례는 Go가 다중 OS 제품 배포를 가로막지 않는다는 근거이며, Chun-su가 같은 지원 수준을 이미 갖췄다는 뜻은 아니다. [GitHub CLI 빌드](https://github.com/cli/cli/blob/trunk/docs/install_source.md), [GitHub CLI 배포](https://github.com/cli/cli#installation)

두 언어 모두 모든 컴퓨터에서 무조건 실행되는 것은 아니다. Go는 지원 OS·CPU 조합과 최소 운영체제 조건이 있고, Node 런타임도 자체 플랫폼 지원 범위를 가진다. Go에 C 라이브러리를 연결하면 교차 빌드에 대상 C 컴파일러 등이 필요할 수 있고, Node의 네이티브 모듈도 대상 환경에 맞는 빌드가 필요하다. 현재 문서의 플랫폼 표는 선택할 릴리스의 지원 범위를 대신하지 않으므로 실제 릴리스를 고정할 때 다시 확인한다. [Go 플랫폼](https://go.dev/doc/install/source), [Go C 연동](https://pkg.go.dev/cmd/cgo), [Node 플랫폼](https://github.com/nodejs/node/blob/main/BUILDING.md#platform-list), [Node 네이티브 모듈](https://nodejs.org/api/addons.html)

일반 사용자에게 개발 도구 설치를 요구하지 않는 배포를 목표로 하면 Go가 자연스러운 기본안이다. Node에서도 런타임을 포함할 수 있으나 그 패키징·지원 버전·갱신까지 제품이 관리해야 한다. 반대로 사용자가 이미 Node 기반 도구를 운영하는 개발자라면 npm 배포가 익숙하고 충분히 간편할 수 있다. 이 평가는 설치 경로에 따른 설계 판단이다.

**2-2. AI 생태계 호환성은 연결 방식에 따라 달라진다**

AI 생태계 전체를 하나의 호환성 점수로 비교하면 선택을 잘못할 수 있다. Go에서 TypeScript 라이브러리를 직접 가져다 쓰는 것과, 그 라이브러리로 만든 프로그램에 요청하는 것은 다르다.

- **업무 지침·스키마·리포트:** 파일 형식과 의미를 문서화하면 코어 언어와 독립적으로 전달할 수 있다. 이것이 사용자의 자산을 보존하고 실행기를 바꾸는 데 직접 연결된다.
- **Jira·메일 API:** HTTP 요청과 제공자별 인증을 구현할 수 있으므로 두 언어 모두 후보가 된다. 실제 제공자·권한·동기화 계약을 확인하고 SDK가 필요한 부분을 정한다.
- **외부 AI 실행기:** CLI나 HTTP로 호출하고 결과·취소·오류 계약을 다루는 구조라면 Go도 연결할 수 있다. 실행기마다 필요한 기능이 이 인터페이스로 제공되는지 확인해야 한다.
- **MCP 도구:** Go 공식 SDK도 클라이언트와 서버를 제공한다. Node로 만든 MCP 서버도 지원 프로토콜·전송 방식이 맞으면 Go 클라이언트에서 연결할 수 있다. 그 서버를 로컬에서 실행하는 데 Node가 필요하다면 사용 환경에 해당 런타임은 여전히 필요하다. [공식 MCP Go SDK](https://github.com/modelcontextprotocol/go-sdk), [공식 MCP TypeScript SDK](https://github.com/modelcontextprotocol/typescript-sdk)
- **특정 SDK의 세밀한 기능:** TypeScript 전용 라이브러리의 세션·훅·도구 제어를 코어에서 직접 사용해야 한다면 Node의 이점이 커진다. SDK와 CLI가 필요한 모든 기능에서 동일하다고 가정하면 안 된다.

구체적인 예로 Claude Agent SDK는 Python과 TypeScript 라이브러리를 제공하고, 다른 언어에서는 CLI를 자식 프로세스로 호출하는 경로를 안내한다. Vercel AI SDK는 TypeScript 도구 모음이다. 이런 SDK를 제품의 중심에 넣기로 하면 Node가 연결 코드와 개발 환경을 줄일 수 있다. 이는 해당 제품을 선택했다는 뜻이 아니라 언어 선택이 바뀌는 조건을 보여주는 예다. [Claude Agent SDK](https://code.claude.com/docs/en/agent-sdk/overview), [Vercel AI SDK](https://github.com/vercel/ai/blob/main/packages/ai/README.md)

처음부터 Go 코어와 Node 보조 프로그램을 모두 필수로 만들 필요는 없다. 일부 선택 기능에만 TypeScript SDK가 필요하면 해당 기능을 별도 연결로 둘 수 있다. 핵심 기능 대부분이 그 SDK에 의존한다면 Node 하나로 구성하는 편이 전체 운영을 단순하게 만들 수 있다. 외부 프로세스 분리는 연결 방식이며, 그 자체로 권한 격리를 보장하지 않는다.

**2-3. 여러 사람이 쓰게 하려면 실제로 준비할 부분**

Chun-su의 현재 설계에서 OS 호환성 작업이 필요한 곳은 서비스 등록, 비밀 저장소, 프로세스 종료·취소, 사용자 데이터 경로, 로컬 관리 채널, 실행기 격리다. macOS의 `launchd`와 Keychain을 그대로 사용하는 구현은 Node로 작성해도 Windows나 Linux에서 그대로 동작하지 않는다. 언어 선택과 별개로 OS 연동 부분을 코어에서 분리하는 방향을 권한다. 지원 OS를 이번 검토만으로 추가 확정하거나 각 OS의 구현을 시작하지 않는다.

넓은 배포를 위한 확인 항목은 다음처럼 잡을 수 있다.

1. 지원 OS·CPU·최소 버전과 해당 환경에서 가능한 기능을 명시한다. 수동 실행 지원과 백그라운드 서비스 지원을 구분한다.
2. 사용자가 소스 빌드 없이 설치할 배포물을 제공한다. 배포 경로에 맞는 서명·패키징·업데이트 방식을 정한다.
3. 선택한 AI 실행기·확장 프로그램·인증에 필요한 별도 준비를 진단한다. Go 코어라는 이유로 전체 제품의 외부 런타임 의존성이 사라진다고 설명하지 않는다.
4. 새 환경에서 설치·첫 실행·계정 연결·중단 복구·업데이트를 확인한다. OS별로 검증되지 않은 기능을 지원 완료로 표시하지 않는다.

위 항목은 이후 제품 검증 기준의 제안이며 이번에 수행한 검증이 아니다. 첫 구현에서 모든 플랫폼의 서비스·비밀 저장소를 먼저 완성하면 결과 평가 루프를 늦추므로, 초기 지원 범위를 좁히고 실제 배포 수요에 따라 확대하는 편을 권한다.

**2-4. 현재 요구에 적용한 추천과 선택이 바뀌는 조건**

현재 설명한 Chun-su는 사용자가 프로그램을 설치하고 업무 기준을 파일로 관리하며, 코어가 교체 가능한 AI 실행기에 일을 맡기는 형태다. 이 사용 방식에 맞춰 사용자가 **Go + SQLite를 선택했다.** 배포받은 사용자는 Go를 알거나 설치할 필요가 없도록 만들 수 있고, 업무 자산의 확장도 Go 코드를 수정하는 방식에 묶이지 않는다. 아래 Node 비교는 선택 근거와 향후 재검토 조건으로 보존하며 현재 구현 언어를 다시 미정으로 두지 않는다.

**Node + TypeScript를 우선할 조건**은 개발자가 npm 패키지로 프레임워크를 가져다 쓰고 TypeScript 확장 코드를 작성하는 것이 핵심이거나, 첫 실행기의 필수 기능이 TypeScript SDK에 깊이 의존하는 경우다. 이때는 SDK를 직접 활용하는 편의가 실행 파일 배포의 이점보다 클 수 있다. 웹 화면이 생길 가능성만으로 코어 언어를 Node로 확정할 필요는 없다.

이번 언어 선택의 핵심 근거는 **첫 사용자가 어떻게 설치·확장할지, 첫 AI 실행기의 필요한 기능을 어떤 인터페이스로 호출할지**다. 공식 문서와 설계 적합성을 비교해 Go + SQLite로 진행하기로 했으며, 두 구현의 성능 비교나 다른 OS에서의 실행 검증을 완료한 것은 아니다.

**3. 추천 개발 구성**

| 역할 | 기본안 | 이유 |
|---|---|---|
| 언어·코어 | Go 표준 라이브러리 | `context`, `os/exec`, `net/http`, `database/sql`, `log/slog`, `encoding/json` 등으로 기본 제어 |
| CLI | Cobra | 하위 명령, 도움말, 옵션, zsh 자동완성을 일관되게 제공 |
| SQLite 드라이버 | `modernc.org/sqlite` | SQLite 부분을 CGo 없이 프로그램에 포함. 별도 SQLite 서버·CLI 설치 불필요 |
| DB 접근 | `database/sql`과 명시적인 SQL | 작업 상태와 트랜잭션이 드러나도록 구성. 초기 ORM은 도입하지 않음 |
| 영속 큐 | 같은 SQLite 안의 작업·시도 기록 | 대기·재시도·복구 상태를 디스크에 남김 |
| 설정·업무 자산 | CLI가 관리하는 JSON, 사람이 작성하는 Markdown | 일반 설정은 표준 JSON 파서, 업무 판단과 예시는 파일로 관리 |
| 결과 검증 | 업무별 JSON Schema와 결정적 검사 | JSON Schema 라이브러리 후보는 `santhosh-tekuri/jsonschema/v6`. 패키지 버전과 계약은 구현 시 고정 |
| 백그라운드 실행 | macOS 사용자용 LaunchAgent | OS의 서비스 관리 기능 사용 |
| 비밀정보 | macOS Keychain | 실제 토큰과 일반 설정·로그 분리. 구체적인 Go 연결 방식은 빌드 조건 확인 후 결정 |
| AI 실행 | 외부 실행기 하나와 어댑터 | 지침·도구·산출물 계약 유지, 실행기 교체 가능 |

Cobra는 CLI 라이브러리로 명령·도움말·셸 자동완성을 제공한다. 웹 프레임워크가 필요한 구성은 아니다. [Cobra](https://github.com/spf13/cobra)

`modernc.org/sqlite`는 CGo 없는 `database/sql` 드라이버이며 macOS arm64를 지원한다. 실제 사용할 릴리스의 Go 요구 버전, 포함된 SQLite 버전, 관련 `modernc.org/libc` 버전 조합을 함께 확인하고 `go.mod`·`go.sum`으로 고정한다. 전체 애플리케이션의 모든 OS 연동까지 CGo가 필요 없다고 보장하는 것은 아니다. Keychain 연결 등 네이티브 부분은 별도 확인 대상이다. [드라이버 문서](https://pkg.go.dev/modernc.org/sqlite), [JSON Schema 라이브러리](https://github.com/santhosh-tekuri/jsonschema)

초기에는 Redis, PostgreSQL, 별도 메시지 브로커, 대형 워크플로 엔진, 웹 관리 화면을 설치하지 않는 안으로 잡는다. 이를 통해 줄이려는 것은 사용자에게 필요한 서비스 설정과 운영 대상이다. 필수 라이브러리는 개발 시 받아 실행 파일에 포함한다.

**4. 한 프로그램의 두 실행 방식**

```text
터미널의 chunsu 명령
  ├─ 수동 실행: 같은 코어를 잠시 실행하고 종료
  └─ 서비스 운영: 로컬 관리 채널로 실행 중인 코어에 요청

Chun-su 코어
  ├─ 업무 정의·입력 준비
  ├─ SQLite 작업 큐·실행 상태
  ├─ AI 실행기 어댑터·허용 도구
  ├─ 산출물·호스트 증거 수집
  ├─ 검증·결과 제공
  └─ 결과 평가·변경 기록

macOS launchd
  └─ 정기 운영에서 같은 chunsu 실행 파일을 관리
```

초기에는 필요할 때 실행하고 끝나는 방식으로 전체 결과 피드백 루프를 검증한다. 정기 운영을 시작하면 `serve` 모드가 같은 코어를 계속 실행한다. 별도의 서비스 프로그램이나 다른 언어의 API 서버를 추가하지 않는다.

같은 데이터 영역을 운영하는 코어는 동시에 하나만 허용한다. 서비스가 이미 실행 중이면 CLI는 사용자 전용 로컬 소켓으로 요청하고 DB를 직접 수정하지 않는다. 수동 실행도 같은 소유권 확인과 파일 잠금을 사용한다. 기본 관리 API는 외부 TCP 포트로 공개하지 않는다.

CLI 관리 채널과 AI 실행기의 업무 도구 채널은 분리한다. 관리 채널의 경로나 Unix 소켓 파일 권한만으로 같은 사용자 권한의 AI 프로세스를 격리했다고 간주하지 않는다. 실제 실행기의 접근 제한까지 확인해야 한다.

**5. 파이프라인과 큐의 최소 범위**

초기에는 한 번에 한 AI 업무를 수행하는 설정을 기본으로 둔다. 대기·조회·취소·로그 확인은 계속 처리할 수 있어야 한다. 작업 수와 시간·재시도 한도는 이름이 있는 설정으로 관리한다.

작업 큐에는 요청 식별자, 업무 종류, 현재 단계와 상태, 실행 가능 시각, 시도 이력, 적용 기준·입력 버전, 대기 사유, 산출물 참조를 보존한다. 정확한 테이블과 필드는 결과·평가 계약을 확정할 때 정한다.

처리 흐름은 다음과 같다.

1. 요청을 SQLite에 기록하고 실행 가능한 대기 작업을 선택한다.
2. 짧은 트랜잭션에서 작업과 새 시도의 소유권을 기록한다.
3. DB 트랜잭션을 닫은 뒤 입력 수집·AI 실행·외부 조회를 수행한다.
4. 산출물과 호스트 증거를 고정하고 해당 시도에 연결한다.
5. 최소 검증·부분 완료·결과 제공 정책에 따라 상태를 기록한다.
6. 재시도나 사람 확인이 필요하면 다음 조건과 재개 지점을 저장한다.

Go의 채널이나 메모리 목록은 실행 중 깨우기와 조정에 사용할 수 있지만 대기열의 원본으로 사용하지 않는다. AI가 응답하는 동안 DB 쓰기 트랜잭션을 유지하지 않는다.

기본 큐 제어에는 다음을 포함한다.

- 프로세스 재시작 뒤에도 남는 대기 작업과 제한된 재시도.
- 작업 취소, 시간 제한, 인증·사람 답변·서비스 복구 대기.
- 같은 요청의 중복 접수 방지와 처리 단계별 진행 기록.
- 이전 실행 인스턴스·시도의 늦은 결과가 새 작업 상태를 덮어쓰지 못하는 확인.
- 중단된 AI 프로세스와 이미 수집된 결과를 확인한 뒤 재개하는 절차.

처음부터 여러 호스트의 분산 리스 체계를 구현하지 않는다. 한 호스트의 단일 소유자, 시도 식별자, 복구 기록부터 검증한다. 리포트 전달 같은 외부 효과가 있으면 결과 불명 상태에서 전체 작업을 무조건 다시 실행하지 않는다. 정확히 한 번 실행을 보장한다고 주장하지 않는다.

결과 평가와 optimization 검토는 업무 실행 기록에 각각 연결한다. 결과 평가가 모든 업무의 필수 통과 게이트가 되거나 optimization이 모든 실행 뒤에 의무적으로 돌아가는 구조로 만들지 않는다. 첫 개발에는 결과 평가·개선·재검증을 포함하고, 심층 optimization 자동화는 이후에 확장한다.

**6. SQLite와 파일의 역할**

SQLite에는 작업 상태·시도·이벤트·자료 식별자·동기화 위치·전달 기록·평가·변경 이력의 연결을 둔다. 원문·첨부·실행 패키지·큰 도구 응답·리포트는 파일로 보존하고 DB에서 참조한다. 업무 기준 원본과 평가 정답도 실행기에게 전달하는 범위와 분리한다.

SQLite 운용의 기본안은 로컬 디스크, WAL 모드, 짧은 트랜잭션, 한 코어의 쓰기 관리다. WAL도 한 번에 한 쓰기만 가능하고 네트워크 파일시스템에 맞지 않는다. 실행 중인 DB를 클라우드 동기화 폴더에 두는 방식은 기본 운영 경로로 선택하지 않는다. [SQLite WAL](https://sqlite.org/wal.html)

초기 내구성 설정은 `synchronous=FULL`을 권하고, 외래 키 검사와 제한된 잠금 대기를 명시적으로 설정한다. 구체적인 대기 시간·보존 한도는 프로필로 관리한다. SQLite CLI의 버전과 앱에 포함된 엔진 버전은 다를 수 있으므로 진단 명령은 앱의 연결에서 실제 엔진과 설정을 확인한다.

파일과 DB를 함께 사용할 때는 파일 고정 후 DB 참조를 기록하는 순서를 정하고, 중간에 종료되어 참조되지 않는 파일이 생긴 경우 복구에서 구분한다. 파일 쓰기·동기화·DB 커밋 사이의 실제 복구는 구현 단계의 검증 항목이다.

DB 구조 변경은 실행 파일에 포함한 버전별 변경 절차로 관리한다. 첫 초기화와 호환 가능한 변경은 프로그램이 수행하고, 데이터를 지우는 초기화나 호환되지 않는 변경을 일반 실행의 부수 효과로 만들지 않는다. 이전 실행 파일이 더 새로운 DB를 이해하지 못하면 명확하게 중단한다.

백업은 사용자 명령으로 제공한다. DB만 복사하면 입력과 리포트가 빠지므로 기준·설정·DB·관련 산출물을 일관된 시점의 묶음으로 보존해야 한다. 초기안은 새 작업 시작을 멈추고 진행 중인 시도의 수집·종료를 정리한 뒤 기록을 고정해 백업하고 운영을 재개하는 방식이다. 활성 WAL의 본체 파일만 복사하는 방법은 사용하지 않고 SQLite의 백업 기능이나 정상 종료 후 복사를 사용한다. 백업에 포함할 파일 목록을 남기고 임시 파일과 백업 폴더 자체는 중복 포함하지 않는다. 실제 복원도 확인해야 한다. 비밀값은 일반 백업에 포함하지 않으며 다른 호스트에서는 연결을 다시 설정할 수 있게 한다. [SQLite 백업](https://sqlite.org/backup.html)

**7. 편하게 설정하는 터미널 흐름**

아래는 구현 후 제공할 명령의 설계 예시다. 현재 이 명령이 설치되어 있거나 실행 가능하다는 뜻은 아니다.

```text
chunsu setup
chunsu doctor
chunsu run mail-review
chunsu report latest
```

`setup`은 한 번의 안내 흐름에서 다음을 처리한다.

1. 데이터 위치와 기존 설정을 찾고 필요한 디렉터리·초기 DB를 만든다. 반복 실행해도 기존 업무 기준이나 결과를 덮어쓰지 않는다.
2. 설치된 실행기 후보와 실제 경로를 확인하고 사용할 실행기를 선택한다.
3. 업무 기준·스키마·예시를 준비한다. 개인 기준이 없는 항목은 임의의 조직 규칙으로 채우지 않고 준비 상태를 표시한다.
4. 저장된 사례를 이용할지 실제 계정을 연결할지 선택한다. 제공자별 연결은 필요할 때 추가한다.
5. 계정 연결 시 일반 설정과 비밀값을 분리해 저장하고 실제 읽기 범위를 확인한다.
6. 결과를 확인할 위치를 보여주고 수동 실행 준비 여부를 진단한다. 정기 서비스 등록은 별도 선택으로 둔다.

`doctor`는 경로, 지원 버전, 설정 참조, DB 접근, 인증 상태와 필요한 실행기 기능을 진단한다. 오프라인 사례 실행에 메일 계정을 필수로 요구하지 않고, 요청한 모드의 부족한 조건만 설명한다. 빌드된 프로그램의 운영 모드에서는 Go 개발 도구와 SQLite CLI를 필수 조건으로 검사하지 않는다. 진단 통과와 업무 품질·격리 검증 통과는 다른 기록이다.

평소 작업은 다음과 같은 CLI로 확인할 수 있게 한다.

```text
chunsu jobs
chunsu show JOB_ID
chunsu logs JOB_ID
chunsu retry JOB_ID
chunsu resolve JOB_ID
chunsu evaluate JOB_ID
chunsu backup
```

작업 ID는 실제 값으로 지정한다. `evaluate`는 해당 요청·입력·평가 기준에 연결해 판정하며, 새 업무에 확인된 기대 조건이 없으면 그 범위를 표시한다. 리포트와 확인 질문은 터미널에서 읽고 답할 수 있게 하며 `--json` 출력으로 후속 자동화에 연결하는 안을 권한다. 첫 로컬 실행에서는 터미널과 저장된 Markdown을 결과 제공 경로로 검증한다. 이동 중 전달 채널은 사용자가 고른 뒤 추가한다.

서비스 운영이 필요해지면 다음 단계를 제공한다.

```text
chunsu service install
chunsu service start
chunsu service status
chunsu schedule configure mail-review
```

서비스 설치는 사용자용 설정을 생성하고, 시작·중지·제거도 같은 CLI에서 처리한다. 관리자 권한의 시스템 서비스는 기본안에 포함하지 않는다. 바이너리와 실행기의 실제 경로를 기록해 터미널의 셸 초기화나 현재 프로젝트 디렉터리에 의존하지 않게 한다.

계정의 최초 OAuth 로그인·동의와 Keychain 접근 확인에는 제공자 브라우저나 OS 창이 필요할 수 있다. 반복 실행·상태 확인·설정·리포트 확인은 터미널 중심으로 운영한다. 토큰을 명령줄 인자, 일반 설정, AI 대화에 입력하도록 설계하지 않는다.

**8. launchd의 역할과 로컬 운영의 한계**

`launchd`는 프로그램 시작·종료·재시작을 담당하고, Chun-su의 스케줄러는 어떤 업무를 언제 실행할지와 미처리 범위를 관리한다. 작업별로 여러 cron·LaunchAgent를 만들기보다 하나의 서비스가 저장된 일정에 따라 작업을 접수하는 안이다.

Apple의 사용자용 LaunchAgent는 해당 사용자의 로그인 세션에서 동작한다. macOS의 절전·종료 중에도 AI 작업이 계속 실행되는 것은 아니다. 깨어난 뒤의 미실행 일정은 업무별 정책에 따라 재개해야 한다. 첫 정책 후보는 누락된 입력 범위를 포함해 현재 시점의 리포트 한 번으로 합치는 방식이며, 시간별 과거 보고서가 필요하면 별도 정책으로 정한다. [Apple launchd 안내](https://developer.apple.com/library/archive/documentation/MacOSX/Conceptual/BPSystemStartup/Chapters/CreatingLaunchdJobs.html)

현재 Mac의 `launchctl`·`launchd.plist` 설명서에서도 서비스 등록·해제와 예약 실행의 절전 동작을 확인했다. 실제 서비스 등록이나 재로그인·절전·복구 검증을 수행한 것은 아니다. 항상 켜진 상태의 실행이 필요해지면 호스트 운영 조건을 별도로 맞춘다.

**9. 실행기 격리는 남겨야 할 별도 결정**

Go 코어를 가볍게 만드는 선택은 사람의 기준 소유권과 실행기 밖의 통제를 약화시키는 이유가 될 수 없다. 같은 사용자로 실행한 자식 프로세스는 그 자체로 격리된 환경이 아니다.

첫 어댑터를 고를 때 지침·입력·결과 경로뿐 아니라 활성 기준, 평가 정답, 다른 실행 기록, Keychain, 관리 채널과 임의 네트워크 접근에 대한 실제 제한 방법을 확인해야 한다. 실행기가 제공하는 격리를 검증해 활용하고, 그것으로 요구 경계를 만들 수 없다면 컨테이너 등 별도 실행 환경을 추가로 선택해야 한다.

따라서 **코어에는 Docker를 필수 설치로 넣지 않고, 실행 환경은 선택한 실행기의 통제 능력에 맞춰 결정한다.** 이 조건이 해결되기 전에는 실제 업무 자료·인증을 연결한 전체 권한 경계가 완성됐다고 표시하지 않는다. Keychain 라이브러리도 비밀값을 외부 명령의 인자로 노출하지 않는지 확인하고 선택한다.

**10. 파일과 코드의 작은 구성안**

앱의 데이터 위치는 OS의 사용자 설정 경로에서 유도하고 명시적인 `CHUNSU_HOME` 재정의를 지원하는 안을 권한다. 아래는 논리적인 배치이며 특정 사용자의 절대 경로를 코드에 고정하지 않는다.

```text
APP_HOME/
  config.json              비밀이 아닌 연결·실행·운영 설정
  workgroups/              사람이 관리하는 업무 정의와 GUIDE.md
  evaluation/              평가 사례·기대 조건, 실행기 가시 범위와 분리
  state/chunsu.db          작업 큐·상태·기록의 연결
  runs/                    입력·도구 증거·산출물
  proposals/               미채택 개선안
  logs/                    민감한 본문과 구분한 운영 로그
  backups/                 일관된 데이터 백업

macOS Keychain             업무 토큰·갱신 토큰
```

이전 제안의 YAML은 필수 요구가 아니므로 초기 일반 설정은 Go 표준 라이브러리로 읽는 JSON을 권한다. 사용자가 주로 작성할 판단 기준과 설명은 Markdown으로 남긴다. 설정 형식의 최종 계약은 실제 예시와 CLI 흐름을 함께 확인한 뒤 고정한다.

```text
cmd/chunsu/                진입점
internal/cli/              터미널 명령
internal/app/              작업 준비·파이프라인·큐·상태 전이
internal/store/            SQLite·버전 변경·파일 참조
internal/executor/         선택한 실행기 어댑터
internal/connectors/       업무별 API·읽기 도구·증거 기록
internal/evaluation/       결과 평가·검토·변경 기록 연결
internal/platform/         macOS 서비스·비밀 저장소 연동
workgroups/                기본 업무 자산과 예시
```

개발 시 Go와 모듈을 사용해 빌드하고, 실제 운영은 빌드한 실행 파일을 사용한다. 다른 Mac에 전달할 때도 대상 OS·CPU에 맞는 실행 파일을 제공하는 방향이다. 프레임워크를 사용하기 위해 Go 개발 도구를 설치하게 할 필요는 없도록 패키징하되, AI 실행기와 계정 인증에 필요한 준비는 별도 안내한다.

**11. 구현 순서와 확인할 증거**

첫 작업은 Go 모듈·CLI·SQLite 기록의 작은 외곽과 메일의 결과 계약·사례를 함께 연결하는 것이다. 상시 서비스부터 완성하고 평가를 나중으로 미루는 순서로 돌아가지 않는다.

1. 지원 Go 패치와 의존성을 고정하고 CLI·초기화·진단·저장 기록을 준비한다.
2. 저장된 사례 → 기준 전달 → 실제 실행기 → 산출물·증거 → 평가·개선·재검증을 수동 실행으로 연결한다.
3. 작업 중단, 잘못된 산출물, 제한된 재시도, 사람 답변과 결과 제공을 같은 기록으로 확인한다.
4. 메일 제공자·조회 범위·비밀 저장소·격리 경계를 확인한 뒤 실제 읽기 전용 연동을 넣는다.
5. 반복 운영이 필요할 때 LaunchAgent와 스케줄을 추가하고 재시작·절전 이후의 재개를 확인한다.
6. Jira 리포트를 같은 코어에 추가해 공통 구조를 검증한다.

가벼움은 빌드 결과 크기, 유휴 상태의 메모리·CPU, 설치 단계, 백그라운드 프로세스 수, 운영자가 다루는 설정 수로 확인한다. AI 실행기의 자원 사용은 코어와 따로 측정한다. SQLite 실제 버전과 잠금·복구·백업·복원, 비밀정보와 활성 기준의 접근 제한, 결과 평가 루프의 실효성도 확인해야 한다.

**이번에 수행한 확인은 로컬 도구의 경로·버전 조회, macOS 로컬 설명서 확인, 공식 Go·Node·SQLite·Python·Rust·Cobra 문서 비교와 설계 작성이다.** 프로그램 빌드·성능 비교·DB 생성·계정 연동·Keychain 저장·서비스 등록·테스트 실행은 하지 않았다. 테스트 코드나 실행용 스키마도 추가하지 않았다.
