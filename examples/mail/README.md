# Synthetic mail inputs

These documents are source fixtures, not generated tests or an approved personal golden set. Addresses use the reserved example.invalid domain. The day snapshot covers combined requests, reference history, a Jira notification, a changed interview time, exclusion, and adversarial source text. The other snapshots preserve successful zero-target collection and known acquisition/content gaps.

Use `chunsu mail inspect INPUT.json` before admission. Do not edit a queued input to change a historical run; queue a new snapshot. Expected semantic judgments belong in the private evaluation store and are never included in an execution package.
